name: ci-golang-workflow
on: [push]
jobs:
  check-application:
  runs-on: ubuntu-latest
  steps:
    - users: actions/checkout@v2
    - users: actions/setup-go@v2
      with:
        go-version: 1.25
    - run: go test
    - run: go tun math.go